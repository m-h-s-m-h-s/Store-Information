# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Set the model (defaults to gpt-5-mini)
OPENAI_MODEL=gpt-5-mini

# Optional: API timeout in milliseconds (defaults to 120000)
API_TIMEOUT=120000

# Optional: Environment (development shows debug logs)
NODE_ENV=production
